# angular-date-time-picker

Lightweight Angular library providing `date-time-picker`, `date-picker`, and `time-picker` components.

## Components

- `<lib-date-time-picker>` — combined date and time picker
- `<lib-date-picker>` — date-only picker
- `<lib-time-picker>` — time-only picker
